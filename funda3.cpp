#include<iostream>
using namespace std;
main()
{
	int a=2000;
	do
	{#include <iostream>
#include <string.h>
using namespace std;
int main()
{
char Str1[100];
    int i;
	cout<<"Enter String  Toggle :  ";    
	gets(Str1);    
	i = 0;    
	while (Str1[i] != '\0')
	{        
		if (Str1[i] >= 'a' && Str1[i] <= 'z') 
		{            
			Str1[i] = Str1[i] - 32;        
		}
		else if 
		(Str1[i] >= 'A' && Str1[i] <= 'Z') 
		{
		    Str1[i] = Str1[i] + 32;
        }
        i++;
    }
cout<<"String after Toggle = "<<Str1;
}
		if(a%4==0)
		{
		cout<<a<<endl;
		}
		a++;
	}
	while(a<=3000);
}

